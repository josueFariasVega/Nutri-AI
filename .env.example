# Plataforma de Nutrición Personalizada - Variables de Entorno
# Copia este archivo como .env y completa con tus valores reales

# Supabase Configuration
VITE_SUPABASE_URL=https://tu-proyecto.supabase.co
VITE_SUPABASE_ANON_KEY=tu_clave_anonima_de_supabase

# Spoonacular API Configuration
VITE_SPOONACULAR_API_KEY=13d415c174a2404daf36bdfb67494012

# Environment Configuration
VITE_APP_ENV=development

# Optional: Analytics and Monitoring (para futuro)
# VITE_ANALYTICS_ID=
# VITE_SENTRY_DSN=

// PRUEBA DIRECTA DE API KEY SPOONACULAR
console.log('=== PRUEBA API KEY SPOONACULAR ===');

// URL de prueba simple (sin parámetros complejos)
const testUrl = 'https://api.spoonacular.com/recipes/complexSearch?apiKey=f51116f447564cff83b9c29ffeb16dca&query=pasta&number=1';

console.log(' Probando URL:', testUrl.replace('f51116f447564cff83b9c29ffeb16dca', 'API_KEY_HIDDEN'));

fetch(testUrl)
  .then(response => {
    console.log(' Código de respuesta:', response.status);
    console.log(' Status text:', response.statusText);
    console.log(' Headers:', Object.fromEntries(response.headers.entries()));
    return response.text();
  })
  .then(data => {
    console.log(' Respuesta completa:', data);
    try {
      const json = JSON.parse(data);
      console.log(' Respuesta JSON:', json);
    } catch (e) {
      console.log(' No es JSON válido');
    }
  })
  .catch(error => {
    console.error(' Error en la prueba:', error);
  });
